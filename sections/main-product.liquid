{%- comment -%}
  Kazan Studio — Main Product Section
  Gallery + details with metafields, add to cart / inquire
{%- endcomment -%}

<section class="product-page section-padding">
  <div class="page-width">
    {%- comment -%} Breadcrumb {%- endcomment -%}
    <div class="product-page__breadcrumb">
      <a href="/collections/all">Work</a>
      {%- for col in product.collections -%}
        {%- unless col.handle == 'all' or col.handle == 'frontpage' -%}
          <span>/</span>
          <a href="{{ col.url }}">{{ col.title }}</a>
          {%- break -%}
        {%- endunless -%}
      {%- endfor -%}
      <span>/</span>
      <span style="color: var(--charcoal);">{{ product.title }}</span>
    </div>

    <div class="product-page__grid">
      {%- comment -%} Gallery {%- endcomment -%}
      <div class="product-page__gallery">
        <div class="product-page__main-image" data-main-image>
          {%- if product.featured_image != blank -%}
            <img
              src="{{ product.featured_image | image_url: width: 1200 }}"
              alt="{{ product.featured_image.alt | escape | default: product.title }}"
              id="main-product-image"
              loading="eager"
            >
          {%- endif -%}
        </div>

        {%- if product.images.size > 1 -%}
          <div class="product-page__thumbnails">
            {%- for image in product.images -%}
              <button
                class="product-page__thumbnail{% if forloop.first %} is-active{% endif %}"
                data-thumbnail
                data-image-url="{{ image | image_url: width: 1200 }}"
                data-image-alt="{{ image.alt | escape }}"
                aria-label="View image {{ forloop.index }}"
              >
                <img
                  src="{{ image | image_url: width: 200 }}"
                  alt="{{ image.alt | escape | default: product.title }}"
                  loading="lazy"
                >
              </button>
            {%- endfor -%}
          </div>
        {%- endif -%}
      </div>

      {%- comment -%} Product Info {%- endcomment -%}
      <div class="product-page__info">
        <h1 class="product-page__title">{{ product.title }}</h1>

        {%- if product.available -%}
          <p class="product-page__price">{{ product.price | money }}</p>
        {%- else -%}
          <p class="product-page__price product-page__price--sold">{{ product.price | money }} — Sold</p>
        {%- endif -%}

        {%- if product.description != blank -%}
          <div class="product-page__description">{{ product.description }}</div>
        {%- endif -%}

        {%- comment -%} Metafields {%- endcomment -%}
        <div class="product-page__meta">
          {%- assign meta_keys = 'material,glaze,technique,edition,dimensions,year' | split: ',' -%}
          {%- assign meta_labels = 'Material,Glaze,Technique,Edition,Dimensions,Year' | split: ',' -%}
          {%- for key in meta_keys -%}
            {%- assign meta_value = product.metafields.custom[key] -%}
            {%- if meta_value != blank -%}
              <div class="product-page__meta-row">
                <span class="product-page__meta-label">{{ meta_labels[forloop.index0] }}</span>
                <span class="product-page__meta-value">{% if key == 'dimensions' %}{{ meta_value }} in{% else %}{{ meta_value }}{% endif %}</span>
              </div>
            {%- endif -%}
          {%- endfor -%}

          {%- assign variant = product.selected_or_first_available_variant -%}
          {%- if variant.weight > 0 -%}
            <div class="product-page__meta-row">
              <span class="product-page__meta-label">Weight</span>
              <span class="product-page__meta-value">{{ variant.weight | weight_with_unit: variant.weight_unit }}</span>
            </div>
          {%- endif -%}
        </div>

        {%- comment -%} Add to Cart / Commission {%- endcomment -%}
        {%- if product.available -%}
          {%- form 'product', product -%}
            <input type="hidden" name="id" value="{{ variant.id }}">
            <div class="product-page__add-to-cart">
              <button type="submit" class="btn btn--primary btn--full">Add to Cart</button>
            </div>
          {%- endform -%}
        {%- else -%}
          {%- assign deposit = product.price | times: 0.5 -%}
          <div class="product-page__add-to-cart">
            <button class="btn btn--secondary btn--full commission-toggle" aria-expanded="false" aria-controls="commission-panel">Commission This Piece</button>
            <div class="commission-panel" id="commission-panel" aria-hidden="true">
              <div class="commission-panel__inner">
                <p class="commission-panel__intro">A commissioned piece will share the same form, technique, and glaze as <em>{{ product.title }}</em> — but clay and fire make each piece its own. Exact replication isn't possible, and that's part of the work.</p>
                <div class="commission-panel__details">
                  <div class="commission-detail">
                    <span class="commission-detail__label">Deposit</span>
                    <span class="commission-detail__value">{{ deposit | money }} (50% upfront)</span>
                  </div>
                  <div class="commission-detail">
                    <span class="commission-detail__label">Timeline</span>
                    <span class="commission-detail__value">2–4 weeks</span>
                  </div>
                  <div class="commission-detail">
                    <span class="commission-detail__label">Balance</span>
                    <span class="commission-detail__value">{{ deposit | money }} due before shipping</span>
                  </div>
                </div>
                {%- form 'contact', id: 'commission-form' -%}
                  <input type="hidden" name="contact[subject]" value="Commission Request: {{ product.title | escape }}">
                  <div class="form-field">
                    <label for="commission-name" class="form-label">Name</label>
                    <input type="text" id="commission-name" name="contact[name]" class="form-input" required>
                  </div>
                  <div class="form-field">
                    <label for="commission-email" class="form-label">Email</label>
                    <input type="email" id="commission-email" name="contact[email]" class="form-input" required>
                  </div>
                  <div class="form-field">
                    <label for="commission-message" class="form-label">Message</label>
                    <textarea id="commission-message" name="contact[body]" class="form-textarea">I'd like to commission a piece similar to {{ product.title | escape }}. I understand a deposit of {{ deposit | money }} is required to begin, with the remaining {{ deposit | money }} due before shipping.</textarea>
                  </div>
                  <button type="submit" class="btn btn--primary btn--full">Send Commission Request</button>
                {%- endform -%}
              </div>
            </div>
          </div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{%- comment -%} Related Products {%- endcomment -%}
{%- assign first_collection = product.collections | where: 'handle', 'all' | first -%}
{%- unless first_collection -%}
  {%- assign first_collection = product.collections.first -%}
{%- endunless -%}

{%- if first_collection and first_collection.products.size > 1 -%}
  <section class="related-products section-padding">
    <div class="page-width">
      <div class="related-products__header">
        <p class="eyebrow">More Work</p>
        <h2>Related Pieces</h2>
      </div>
      <div class="grid grid--{{ section.settings.related_columns }}">
        {%- for related in first_collection.products limit: 4 -%}
          {%- unless related.handle == product.handle -%}
            {% render 'product-card', product: related %}
          {%- endunless -%}
        {%- endfor -%}
      </div>
    </div>
  </section>
{%- endif -%}

{% schema %}
{
  "name": "Product page",
  "settings": [
    {
      "type": "select",
      "id": "related_columns",
      "label": "Related products columns",
      "options": [
        { "value": "3", "label": "3" },
        { "value": "4", "label": "4" }
      ],
      "default": "3"
    }
  ]
}
{% endschema %}
